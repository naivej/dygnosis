### E170: Multiple occbin_constraints blocks

The file has more than one ``occbin_constraints`` block. Dynare refuses: `Multiple 'occbin_constraints' blocks are not allowed`.

**Fix**

Keep a single ``occbin_constraints`` block.
