### E173: bind or relax tag without a name tag

A model equation has a ``bind`` or ``relax`` tag but no ``name`` tag. They refuse: `An equation with a 'bind' or 'relax' tag must have a 'name' tag`.

**Fix**

Add ``[name='…']`` on that equation.
