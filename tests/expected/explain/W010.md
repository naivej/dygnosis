### W010: Parameter declared but never assigned

A name appears in the `parameters` block but no assignment was found in the parameter section or in `steady_state_model`. At runtime, the parameter will be undefined and most computations will fail.

**Fix**

Assign a numerical value, or remove the declaration if the parameter is no longer used.
