### E393: Shock skewness declared twice

The same single-shock skewness is specified twice within one `shocks` block. Dynare refuses: `shocks: skewness of e declared twice`. A later `shocks` block may specify it again.

**Fix**

Keep one `skew e` row in each block.
