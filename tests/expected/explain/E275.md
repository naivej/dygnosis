### E275: Namespace-qualified symbol

An expression uses ``ident.ident`` outside a skipped ``shock_paths`` body. Dynare refuses: `Namespace-qualified symbol {ns}.{name} not allowed in this context`.

**Fix**

Use a declared symbol without a namespace prefix.
