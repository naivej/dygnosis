### E271: Option declared twice in one list

The same option identifier appears twice in one ``(…)`` list. Dynare refuses: `option {name} declared twice`.

**Fix**

Keep one copy of that option in the list.
