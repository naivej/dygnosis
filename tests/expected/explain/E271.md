### E271: Option declared twice in one list

The same option identifier appears twice in one `(…)` list. Dynare refuses: `option {name} declared twice`. The adjacent option lists of `mshocks` and `shock_paths` use Dynare's other sentence: `The '{name}' option is declared multiple times`.

**Fix**

Keep one copy of that option in the list.
