### W121: Parameter used with a lead or lag

A declared parameter appears with a time subscript such as ``beta(+1)`` or ``rho(-1)`` in the model block. They WARN: `The following parameter(s) are used with a lead or a lag: betta`.

**Fix**

Remove the time index, or declare the symbol with ``var`` / ``varexo`` if it really is a variable.
