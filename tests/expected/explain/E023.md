### E023: Predetermined variable not declared endogenous

A name listed in `predetermined_variables` must also be declared as an endogenous variable in the `var` block. Dynare requires the variable to exist before it can be marked predetermined.

**Fix**

- Add the variable to the `var` declaration, or
- Remove it from `predetermined_variables` if it is not actually   endogenous
