### W056: Duplicate parameter assignment

The same parameter is assigned a value more than once in the parameter section. Only the last assignment takes effect at runtime, so the earlier one is silently ignored — usually a bug.

**Fix**

Remove one of the assignments, or rename if two distinct parameters were intended.
