### E258: Several varobs statements

The file has more than one ``varobs`` statement. Dynare refuses: `varobs: you cannot have several 'varobs' statements in the same MOD file`.

**Fix**

Keep a single ``varobs`` statement.
