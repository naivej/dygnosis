### E243: histval pair declared twice

The same ``(name, lag)`` pair appears twice in ``histval``. Dynare refuses: `hist_val: ({name}, {lag}) declared twice`.

**Fix**

Keep one assignment for that pair.
