### E243: histval pair declared twice

The same ``(name, lag)`` pair appears twice in one ``histval`` block. Dynare refuses: `histval: {name}({lag}) declared twice`.

**Fix**

Keep one assignment for that pair in the block.
