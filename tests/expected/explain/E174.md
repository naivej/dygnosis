### E174: Missing bind expression

A named OccBin constraint has no ``bind`` inequality (a non-comparison such as ``bind i;`` counts as missing). They refuse: `The 'bind' expression is missing in constraint 'NAME'`.

**Fix**

Add a ``bind`` inequality on that constraint.
