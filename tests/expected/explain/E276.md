### E276: log of interned numeric zero

``log`` or ``ln`` is applied to interned numeric 0 while the expression is built. Dynare refuses: `log(0) not defined!`.

**Fix**

Do not take the log of a literal zero.
