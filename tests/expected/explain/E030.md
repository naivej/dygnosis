### E030: Duplicate declaration across types or `#` twice

The same identifier is declared in two different blocks (for example both `var` and `varexo`), or a model-local `#` name is defined twice. They refuse: `Symbol y declared twice with different types!` and `Local model variable foo declared twice.`

**Fix**

Remove the extra declaration. If you intended two related but distinct symbols, rename one.
