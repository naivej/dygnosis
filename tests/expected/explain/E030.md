### E030: Duplicate declaration across blocks

The same identifier is declared in more than one block (for example, in both `var` and `varexo`, or twice in `parameters`). Dynare requires each name to belong to exactly one symbol class.

**Fix**

Remove the duplicate declaration. If you intended two related but distinct symbols, rename one (the LSP's rename action propagates the change across the file).
