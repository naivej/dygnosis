### S063: Heterogeneous operator inside epilogue

Dynare refuses: `… forbidden in epilogue` / `… because it is heterogeneous`. Catching step: check for the remaining heterogeneous case. The three named VAR/PAC operators shipped at parse in 0.8 as E445. Owner: skip 0.9 E (heterogeneous). This code is never emitted.
