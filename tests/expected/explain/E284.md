### E284: @#for tuple arity mismatch

An ``@#for`` tuple has a different size from the index list. Dynare refuses: `Encountered tuple of size {n} but only have {m} index variables`.

**Fix**

Match the number of index names to the tuple size.
