### E475: SUM inside a heterogeneous model

``SUM`` appears inside ``model(heterogeneity=…)``. Dynare refuses: `The SUM() operator cannot be used inside a model(heterogeneity=...) block`.

**Fix**

Use ``SUM`` only in the aggregate model.
