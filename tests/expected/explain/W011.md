### W011: Parameter assignment cannot be evaluated

An assignment like `phi = 1/(1-beta)` could not be evaluated because one or more right-hand-side symbols are not yet defined. The parameter falls back to undefined.

**Fix**

Reorder the parameter section so that dependencies appear before dependents.
