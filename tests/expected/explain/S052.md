### S052: shock_paths body, DATE, and related parse

Dynare refuses: `various`. Catching step: parse. Owner: skip 0.11 E (DATE / `set_time` / `database` / `shock_paths` body / `mshocks` add-multiply / surprise `stderr` / `heteroskedastic_shocks`). This code is never emitted.
