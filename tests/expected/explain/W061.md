### W061: Ambiguous include parent context

The active include file is reachable from more than one parent model, so the editor cannot tell which parent's declarations apply.

**Fix**

Open the intended parent `.mod` file.
