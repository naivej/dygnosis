### W061: Ambiguous include parent context

The active include file is reachable from more than one parent model, so the language server cannot safely infer which parent declarations and block context should apply.

**Fix**

Open or run the intended parent `.mod` file, or provide only that parent and its include closure when calling workspace tools.
