### E204: osr has both optim_weights and planner_objective

They refuse: `The osr statement cannot have both optim_weights and a planner_objective; they are mutually exclusive.`

**Fix**

Keep either ``optim_weights`` or ``planner_objective``, not both.
