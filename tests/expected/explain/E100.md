### E100: Optimal-policy command requires a planner_objective

``ramsey_model``, ``ramsey_policy``, and ``discretionary_policy`` optimise a planner's loss function, so they require a ``planner_objective`` statement, which is missing.

**Fix**

Add a ``planner_objective <expression>;`` statement before the policy command.
