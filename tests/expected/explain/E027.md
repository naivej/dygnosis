### E027: varexo_det with Ramsey

``ramsey_model`` and ``ramsey_policy`` cannot be used with deterministic exogenous variables. They refuse: `ramsey_model and ramsey_policy are incompatible with deterministic exogenous variables`.

**Fix**

Remove the ``varexo_det`` declaration, or drop the Ramsey command.
