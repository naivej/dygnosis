### E344: conditional_forecast_paths variable declared twice

One ``var`` name appears twice in a single ``conditional_forecast_paths`` block. Dynare refuses: `shocks/conditional_forecast_paths: variable Pie declared twice`.

**Fix**

Merge the two rows into one, or remove the duplicate.
