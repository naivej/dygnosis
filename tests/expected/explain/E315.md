### E315: optim_weights variable declared twice

One ``optim_weights`` block gives the same variable two weights. Dynare refuses: `optim_weights: y declared twice`.

**Fix**

Keep one weight per variable in the block.
