### E176: Constraint listed in both bind and relax

The same constraint name appears in both the ``bind`` and ``relax`` tags on one equation. They refuse: `The constraint 'C' is both in the 'bind' and 'relax' tags`.

**Fix**

Keep the name in ``bind`` or ``relax``, not both.
