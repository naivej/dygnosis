### E316: optim_weights pair declared twice

``optim_weights`` gives the same pair of variables two weights. Dynare refuses: `optim_weights: pair of variables (y, z) declared twice`.

**Fix**

Keep one weight per pair (the order of the two names matters).
