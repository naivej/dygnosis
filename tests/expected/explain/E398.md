### E398: Surprise shocks uses add or multiply

A `shocks(surprise)` row uses `add` or `multiply`. Dynare refuses: `shocks(surprise): 'add' keyword not allowed` (or `multiply` in place of `add`).

**Fix**

Use `values` for the surprise shock.
