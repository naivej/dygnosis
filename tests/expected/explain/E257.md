### E257: Default equation tag collides with existing name

An equation has no ``name`` tag, and both the LHS identifier and the 1-based equation index are already used as ``name`` values. Dynare refuses: `Error creating default equation tag: cannot assign default tag to equation number {n} because it is already in use`.

**Fix**

Give the equation an explicit ``[name=…]`` that does not collide.
