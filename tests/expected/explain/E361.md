### E361: svar_identification name added twice in one equation row

One ``equation N, name…;`` row lists the same name twice. Dynare refuses: `Py restriction added twice.`

**Fix**

List the name once.
