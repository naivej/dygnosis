### W054: Duplicate equation

Two equations inside the `model` block are textually identical. The diagnostic cites the line where the duplicate appears and the line where the first occurrence was found.

**Fix**

Remove the duplicate. Dynare would otherwise report an equation-count mismatch (E010) downstream.
