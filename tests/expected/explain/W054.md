### W054: Duplicate equation

Two equations inside the `model` block are textually identical. The diagnostic cites the line where the duplicate appears and the line where the first occurrence was found. This is an extra Warning: they accept duplicate equations. A duplicate can also show up as our equation-count Warning `W013`.

**Fix**

Remove the duplicate.
