### E397: mshocks uses add or multiply

A `mshocks` row uses `add` or `multiply`. Dynare refuses: `mshocks: 'add' keyword not allowed` (or `multiply` in place of `add`).

**Fix**

Use `values` in `mshocks`.
