### E313: filter_initial_state entry declared twice

The same ``(name, lag)`` pair appears twice in ``filter_initial_state``. Dynare refuses: `filter_initial_state: (y, 0) declared twice`.

**Fix**

Keep one assignment per ``(name, lag)`` pair.
