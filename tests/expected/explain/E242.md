### E242: histval lag greater than zero

A ``histval`` assignment uses a positive lag. Dynare refuses: `histval: the lag on {name} should be less than or equal to 0`.

**Fix**

Use a lag of 0 or less.
