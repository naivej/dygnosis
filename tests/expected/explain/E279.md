### E279: external_function name used as a variable outside model

A name declared with ``external_function(name=…)`` is used as a bare variable outside ``model``. Dynare refuses: `Symbol '{name}' is the name of a MATLAB/Octave function, and cannot be used as a variable.`

**Fix**

Call the function with arguments, or use a different name.
