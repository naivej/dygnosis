### W057: Stray equation outside model block

A line that looks like a model equation appears outside the `model` ... `end;` block. This is an extra Warning: they accept a stray top-level equation such as `0 = 1` at check.

**Fix**

Move the equation inside the `model` block, or convert it to a parameter assignment if it belongs at the top level.
