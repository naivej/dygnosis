### E282: Model-local used outside model

A ``#`` model-local name is used outside ``model`` (not in initval/endval/histval). Dynare refuses: `Variable {name} not allowed outside model declaration. Its scope is only inside model.`

**Fix**

Keep the name inside ``model``, or declare it as a parameter.
