### W053: Parameter assigned in initval/endval is ignored

A parameter is assigned a value inside an `initval`/`endval` block, where Dynare ignores it. Assign parameters before the model block, or inside `steady_state_model`, instead.
