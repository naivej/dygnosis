### E281: Mod-file local used inside model

A name auto-declared outside ``model`` is used inside ``model``. Dynare refuses: `Variable {name} not allowed inside model declaration. Its scope is only outside model.`

**Fix**

Declare the name as ``var``, ``varexo``, or ``parameters`` if it belongs in the model.
