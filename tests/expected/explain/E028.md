### E028: varexo_det with identification

The ``identification`` command cannot be used with deterministic exogenous variables. They refuse: `identification is incompatible with deterministic exogenous variables`.

**Fix**

Remove the ``varexo_det`` declaration, or drop ``identification``.
