### E130: Variable used before assignment in steady_state_model

The ``steady_state_model`` block is evaluated top to bottom as a sequence of assignments, so every variable on a right-hand side must already have been assigned above. A variable is referenced before its own assignment, which the Dynare preprocessor rejects.

**Fix**

Reorder the assignments so each variable is computed before it is used.
