### W150: Deprecated command or option

A deprecated command or option is used; current Dynare warns and may remove it in a future release. Commands: ``simul`` → ``perfect_foresight_setup`` + ``perfect_foresight_solver``; ``ramsey_policy`` → ``ramsey_model`` + ``stoch_simul``. Options: ``aim_solver`` → ``dr = aim``; ``bytecode`` (being removed).

**Fix**

Switch to the modern command or option form.
