### E277: log10 of interned numeric zero

``log10`` is applied to interned numeric 0 while the expression is built. Dynare refuses: `log10(0) not defined!`.

**Fix**

Do not take log10 of a literal zero.
