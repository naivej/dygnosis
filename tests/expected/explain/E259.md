### E259: Several varexobs statements

The file has more than one ``varexobs`` statement. Dynare refuses: `varexobs: you cannot have several 'varexobs' statements in the same MOD file`.

**Fix**

Keep a single ``varexobs`` statement.
