### E178: shocks(surprise) without occbin_constraints

A ``shocks(surprise)`` block requires an ``occbin_constraints`` block. They refuse: `the 'shocks(surprise)' block can only be used in conjunction with the 'occbin_constraints' block.`.

**Fix**

Add ``occbin_constraints``, or drop the ``surprise`` option.
