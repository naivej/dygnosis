### E285: Macro + operand type mismatch

A macro ``+`` combines operands of incompatible types. Dynare refuses: `Type mismatch for operands of + operator`.

**Fix**

Add numbers to numbers, or change the operands.
