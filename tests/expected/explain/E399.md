### E399: Surprise shock period is a date

A `shocks(surprise)` period is written as a DATE. Dynare refuses: `shocks(surprise): dates are not allowed in the 'periods' keyword`.

**Fix**

Use an integer period or integer range.
