### E395: Period range runs backwards

An integer period range starts after it ends. Dynare refuses: `Can't have first period index greater than second index in range specification`.

**Fix**

Put the earlier period first.
