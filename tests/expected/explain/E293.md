### E293: SUM in the epilogue block

The ``epilogue`` block uses the ``SUM`` operator. Dynare refuses: `The SUM() operator is forbidden in epilogue block`.

**Fix**

Remove the operator from the epilogue expression.
