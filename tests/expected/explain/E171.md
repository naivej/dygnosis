### E171: Too many constraints in occbin_constraints

An ``occbin_constraints`` block lists more than two named constraints. They refuse: `only up to two constraints are supported in 'occbin_constraints' block`.

**Fix**

Remove the extra constraint. OccBin supports at most two.
