### W031: Symbol declared twice with the same type

The same identifier is declared more than once in `var`, `varexo`, `varexo_det`, or `parameters`. They accept and WARN `Symbol y declared twice.`

**Fix**

Remove the redundant declaration.
