### W031: Symbol declared twice with the same type

The same identifier is declared more than once in `var`, `varexo`, `varexo_det`, or `parameters`. Dynare accepts and warns `Symbol y declared twice.`

**Fix**

Remove the redundant declaration.
