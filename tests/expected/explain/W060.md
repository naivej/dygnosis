### W060: Exogenous variables declared but no shocks block

One or more exogenous variables are declared in `varexo` but the file contains no `shocks` block specifying their variance-covariance structure. The model is then deterministic.

**Fix**

Add a `shocks` block to define the shock processes, or remove the unused `varexo` declarations.
