### E452: Nonstationary PAC target has no matching block

A written model equation uses pac_target_nonstationary(NAME) without matching pac_target_info(NAME). Dynare refuses at transform: `the pac_target_nonstationary operator does not match a corresponding 'pac_target_info' block`.

**Fix**

Add the matching target-info block or correct the operator name.
