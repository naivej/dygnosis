// inventory: lists_e239_forecast_upper
// The statement head is cased up; 7.1 prints its own lowercase word anyway.
var y c;
varexo e;
varexo_det ed;
parameters a;
a = 0.5;

model;
y = a*y(-1) + e;
c = y;
end;

FORECAST z;
